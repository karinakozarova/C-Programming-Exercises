#include <stdio.h>
// declaration of the funcs if they are after main
//data_type name(parametres){}
int intFunc();
void voidFunc();
float floatFunc();

int main(int argc, char const *argv[])
{
	//main function, can't be compiled without it
	return 0;
}

int intFunc(){
	return 0; // 0 is int
}

void voidFunc(){
	//returns nothing
}

float floatFunc(){
	//returns float
}